# Day 3 Homework Submission — Weather MCP Server + Agent

## Source repository

Repository:

`https://github.com/PedroD1265/dataexpert-weather-vector-retrieval`

Branch:

`day3-weather-mcp-agent`

Submission project folder:

`day3-weather-mcp-agent/`

## Databricks MCP App

Planned app name:

`mcp-weather-intelligence`

App URL after deployment:

`TO_BE_ADDED_AFTER_DEPLOYMENT`

MCP endpoint:

`TO_BE_ADDED_AFTER_DEPLOYMENT/mcp`

## Databricks Agent

Planned agent name:

`weather-intelligence-agent`

System prompt:

`agent/SYSTEM_PROMPT.md`

Enabled tools:

- `get_current_weather`
- `get_forecast`
- `get_travel_recommendation`
- `compare_weather`

## Evidence required before final ZIP

- MCP Databricks App running successfully.
- MCP tool discovery showing the weather tools.
- Agent configuration/system prompt.
- Demo 1: current weather question + tool call + final answer.
- Demo 2: multi-day forecast question + tool call + final answer.
- Demo 3: recommendation question + tool call + final answer.
- Optional stretch: compare multiple cities.

## Security

Open-Meteo requires no API key. No passwords, API keys, tokens, or secret values are included in source control or the submission.
